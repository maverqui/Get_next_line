c static char *str;

void blabla()
{
    static char buff[BUFFER_SIZE];

    
}

int main()
{
    if (fd < 0 || BUFFER_SIZE < 1)
        return (NULL);
}

salut\ncou

buff = ft_substr(lu, ft_strchr(lu, "\n"));
final = lu;
final[ft_strchr + 1] = '\0'
final = salut\n;
buff = cou\0;

while (read(fd, buff, BUFFER_SIZE))
    if (ft_strchr(lu, "\n") == 0)
        ft_strjoin(buff, lu);
    else
        break;
salut\ncoucou\nbonjour\0

salut
coucou
bonjour 